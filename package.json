{
  "scripts": {
    "start": "cd equipment-control-api && cross-env PORT=3000 npm start",
    "build": "cd frontend && npm run build && cd ../equipment-control-api && npm run build",
    "install": "npm install && cd equipment-control-api && npm install && cd ../frontend && npm install"
  },
  "dependencies": {
    "@nestjs/jwt": "^11.0.2",
    "@nestjs/passport": "^11.0.5",
    "bcrypt": "^6.0.0",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
